@import "./App.scss";

.Applet {
    .flex {
        display: flex;
    }
    
    .button {
		font-size: 1rem;
        border: none;
        color: var(--foreground);
        text-align: center;
        transition: 200ms linear; 
    }
    
    .button:hover {
			color: $highlight;
    }
    
    .lorem {
        color: var(--foreground);
        font-size: 1rem;
        overflow: hidden;
    }

	h2 {
		margin-top: 2em;
	}

    width: 100vw;
    position: relative;
	left: 6em;
    background-color: var(--background);
    display: flex;
}
